{
  "name": "palmos-contracts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "PalmOS Mantle contracts (ERC-8004 IdentityRegistry + AgentActionLog). Isolated toolchain: its own local node_modules so it never touches the worktree's symlinked node_modules.",
  "scripts": {
    "compile": "node compile.mjs"
  },
  "dependencies": {
    "solc": "0.8.26"
  }
}
